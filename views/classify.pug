extends layout

block content

    div(class="container-results")
        h2 #{displayMessage}
        if imageClassification
            img(src=`${photoUrl}`, alt="searchedImage" ,width="300px",  height="300px")
            h1 #{imageClassification[0].className}
        
        